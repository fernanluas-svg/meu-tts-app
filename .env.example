# A geração de voz NÃO usa chave de API externa (Gemini/OpenAI).
# O front-end chama a API Python local (Coqui XTTS) em http://127.0.0.1:8000.

# XTTS_API_URL: URL do servidor XTTS local (apenas referência; o código usa o default).
XTTS_API_URL="http://127.0.0.1:8000"